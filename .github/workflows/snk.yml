name: Generate Snk

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * *'

jobs:
  generate_snake:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        ref: output
    - name: Set up Node.js 16
      uses: actions/setup-node@v2
      with:
        node-version: 16
    - uses: Platane/snk@v2
      with:
        github_user_name: ${{ github.repository_owner }}
        outputs: |
          github-contribution-grid-snake.svg?color_snake=#B04078&color_dots=#bfd6f6,#8dbdff,#64a1f4,#4b91f1,#3c7dd9
